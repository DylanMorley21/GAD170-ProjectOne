using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class ProjectOne : MonoBehaviour
{
    int playerHealth = 0;
    int playerVitality = 0;
    int playerStrength = 0;
    int playerDexterity = 0;
    int playerPowerLevel = 0;

    int skeletonHealth = 0;
    int skeletonVitality = 0;
    int skeletonStrength = 0; 
    int skeletonDexterity = 0;
    int skeletonPowerLevel = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
